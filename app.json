{
    "name": "projectapp",
    "scripts": {
      "postinstall": "cd application && npm install && npm run build"
    },
    "env": {
      "NODE_ENV": "production"
    },
    "buildpacks": [
      {
        "url": "heroku/nodejs"
      },
      {
        "url": "https://github.com/mars/create-react-app-buildpack.git"
      }
    ]
  }